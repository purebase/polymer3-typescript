# Polymer 3 Typescript

Basic project structure for a Polymer 3 app written in Typescript

## Install dependencies

`$ yarn install`

## Run in development mode

`$ yarn start`

## Build for production

`$ yarn build`